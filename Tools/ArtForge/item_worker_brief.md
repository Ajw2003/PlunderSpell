You are modelling plunder ITEMS for Plunderspell with the ArtForge tool in /home/user/PlunderSpell. ITEMS ONLY — never build or touch structures or enemies (another agent owns structures), and never touch Tools/EnemyForge, Tools/AssetPipeline or Assets/Models/Enemies. Do NOT git commit or push; an auto-save loop commits Tools/ArtForge, Assets/Models/ArtBible/Items and docs/art/models every 3 minutes. Three other workers are modelling other Ages at the same time in the same checkout: only edit YOUR blueprint module and only build/render with --age <YOUR AGE> (and --only). Do not edit shared framework files (kit.py, materials.py, assemble.py, validate.py, spec.py, build.py, render.py); if the framework truly blocks you, work around it inside your blueprint module and say so in your report.

READ FIRST: Tools/ArtForge/README.md (blueprint API, part kinds box/cyl/cone/sphere/ico/torus/shard + lathe/prism/tube, paint regions, smooth flag, bevel flag, bbox_overrides, family overrides), the two finished examples Tools/ArtForge/art_forge/blueprints/items_bronze.py (sealed-amphora) and items_high.py (gilded-altarpiece), docs/art/BRIEF.md, your Age's JSON docs/art/data/<age>.json (items section), and each item's concept sheet docs/art/concept/<age>/<slug>.png (open it with Read). The finished review sheets docs/art/models/bronze/sealed-amphora.png and docs/art/models/high/gilded-altarpiece.png show the quality bar: the model must read as the concept at game distance — shaped outlines, lathed profiles, painted bands, not a pile of boxes.

FOR EACH ITEM (finish one completely before starting the next, so a cut-off loses little):
1. Write its builder in Tools/ArtForge/art_forge/blueprints/items_<age>.py (create the module if absent; it is auto-discovered). Model from the JSON build bullets + dimensions + concept silhouette; materials from the JSON hex list (extra families only if the build bullets need a material the JSON omits — the tool will warn, name why in a comment). Stay under the JSON triangle budget. Use bbox_overrides only for a real spec ambiguity, with its reason.
2. python3 Tools/ArtForge/build.py items --age <age> --only <slug>   → must PASS.
3. python3 Tools/ArtForge/render.py items --age <age> --only <slug>  → writes docs/art/models/<age>/<slug>.png. Open it with Read, compare to the concept honestly (silhouette, proportions, colours, key details), fix and rebuild until it clearly matches. At least one improvement pass per item.
Renders are slower while other workers render too; that is expected.

WHEN ALL YOUR ITEMS ARE DONE: python3 Tools/ArtForge/build.py items --age <age>  (all of your Age, must all PASS — run it twice), then python3 Tools/ArtForge/render.py items --age <age>. Report: per item slug, triangles/budget, bbox vs spec, PASS/FAIL, what matches the concept and what does not; commands run with their final result lines; anything you could not get right.
