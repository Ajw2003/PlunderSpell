# ArtForge enemy worker brief

You are modelling rigged ENEMIES for Plunderspell with ArtForge in /home/user/PlunderSpell, following docs/art/WORKFLOW.md steps 5–7. ENEMIES ONLY: never build or touch structures (another agent owns them) or items, and never touch Tools/EnemyForge, Tools/AssetPipeline or Assets/Models/Enemies. Do NOT git commit or push; Tools/autosave.sh is committing Tools/ArtForge, Assets/Models/ArtBible/Enemies, the manifest and docs/art/models every 3 minutes. Other workers model other Ages at the same time in the same checkout: edit ONLY your module Tools/ArtForge/art_forge/blueprints/enemies_<age>.py, and build/render ONLY with --age <age> (and --only). Do not edit shared framework files (kit.py, figures.py, rig.py, materials.py, assemble.py, validate.py, spec.py, build.py, render.py); if the framework truly blocks you, work around it inside your module and say so in your report.

READ FIRST: Tools/ArtForge/README.md (all, esp. the enemy path, figures.py API, skinning rules, Traps), the finished samples in Tools/ArtForge/art_forge/blueprints/enemies_high.py (lantern-warden = humanoid, alaunt-hound = quadruped) and their review sheets docs/art/models/high/lantern-warden.png and alaunt-hound.png (the quality bar), docs/art/BRIEF.md, docs/systems/scale.md, your Age's enemies in docs/art/data/<age>.json, and each enemy's concept sheet docs/art/concept/<age>/<slug>.png (open with Read).

FOR EACH ENEMY (finish one completely before the next, so a cut-off loses little):
1. Write its builder using figures.Human (and figures.Quadruped for animals). Model from the JSON build bullets, height and silhouette note, and the concept's front and side views: armour, clothing layers, helmet shape, held weapons/props on Hand/prop bones. The silhouette note says what must read at ten paces; make that read first. Materials from the JSON hex list (extra families only when the bullets need a colour the JSON omits, with a comment why). Obey the pigment rules: no verdigris or lapis on enemies; gold only if it is stealable plunder. Stay under the JSON triangle budget. Height within tolerance of height_m.
2. python3 Tools/ArtForge/build.py enemies --age <age> --only <slug>   → must PASS.
3. python3 Tools/ArtForge/render.py enemies --age <age> --only <slug>  → docs/art/models/<age>/<slug>.png. Open it with Read and compare honestly to the concept (silhouette, proportions, colours, key props). Check the POSED views: the skin must bend without tearing, and props must follow their hands. Fix, rebuild, re-render. At least one improvement pass per enemy.

WHEN ALL ARE DONE: python3 Tools/ArtForge/build.py enemies --age <age> twice (all must PASS both times), then python3 Tools/ArtForge/render.py enemies --age <age>. Report, per enemy slug: triangles vs budget, height vs spec, bone count, PASS or FAIL, what matches the concept and what does not; the commands you ran with their final result lines; and anything you could not get right.
